---
title: 'Spec Templates'
description: 'Coding convention templates for common stacks'
---

{/* markdownlint-disable MD024 */}

## Backend Templates

<Tabs>
  <Tab title="Node.js + Express">
    ### Project Structure

    ```
    src/
    ├── routes/     # Express route handlers
    ├── services/   # Business logic
    ├── db/
    │   ├── models/     # Database models
    │   └── migrations/ # SQL migrations
    ├── lib/        # Shared utilities
    └── types/      # TypeScript types
    ```

    ### Error Handling

    All errors use `AppError` from `src/lib/errors.ts`. Never throw raw `Error` objects.

    ### Database

    - Table names: `snake_case`, plural (`user_sessions`)
    - Column names: `snake_case` (`created_at`)
    - Always include `id`, `created_at`, `updated_at`
    - Use the query builder, not raw SQL

    ### API Routes

    Routes go in `src/routes/`. One file per resource. Validate input with Zod.

    ### Authentication

    JWT tokens for auth. Protected routes use `requireAuth` middleware.

  </Tab>

  <Tab title="Python + FastAPI">
    ### Project Structure

    ```
    app/
    ├── api/
    │   └── routes/     # FastAPI route handlers
    ├── services/       # Business logic
    ├── db/
    │   ├── models/     # SQLAlchemy models
    │   └── migrations/ # Alembic migrations
    ├── core/           # Config, security, dependencies
    └── schemas/        # Pydantic schemas
    ```

    ### Error Handling

    Custom exceptions in `app/core/exceptions.py`. Use the global exception handler.

    ### Database

    - Table names: `snake_case`, plural
    - Use SQLAlchemy ORM, not raw SQL
    - Each model has `id`, `created_at`, `updated_at`

    ### API Routes

    One router per resource in `app/api/routes/`.

    ### Validation

    Use Pydantic schemas for all input/output.

  </Tab>
</Tabs>

---

## Frontend Templates

<Tabs>
  <Tab title="React + TypeScript">
    ### Project Structure

    ```
    src/
    ├── components/  # Reusable components
    ├── pages/       # Route pages
    ├── hooks/       # Custom hooks
    ├── stores/      # Zustand stores
    ├── lib/         # Utilities
    ├── types/       # TypeScript types
    └── api/         # API client functions
    ```

    ### Components

    Named exports only. No default exports. Props interface defined above the component.

    ### State Management

    Use Zustand. Store files in `src/stores/`.

    ### API Calls

    API functions in `src/api/`. Use the fetch wrapper from `src/lib/fetch.ts`.

    ### Styling

    Tailwind CSS. No inline styles. No CSS modules.

  </Tab>
</Tabs>

---

## Full-Stack Templates

<Tabs>
  <Tab title="Next.js + tRPC + Prisma">
    ### Project Structure

    ```
    src/
    ├── app/         # Next.js app router
    ├── server/
    │   ├── api/     # tRPC routers
    │   └── db/      # Prisma client
    ├── components/  # React components
    ├── hooks/       # Custom hooks
    └── lib/         # Shared utilities
    prisma/
    └── schema.prisma  # Database schema
    ```

    ### API (tRPC)

    Routers in `src/server/api/routers/`. Use zod for input validation.

    ### Database (Prisma)

    Models in `prisma/schema.prisma`. Run `npx prisma generate` after schema changes.

    ### Components

    Same as React guidelines. Use tRPC hooks for data fetching.

  </Tab>
</Tabs>

---

## Using These Templates

1. Copy the template to `.trellis/spec/backend/index.md` or `.trellis/spec/frontend/index.md`
2. Adjust paths to match your project
3. Add project-specific conventions
4. Remove sections that don't apply

Templates are starting points. Your spec should reference actual files in your codebase.
