---
title: 'v0.3.5'
description: '2026-03-05'
---

Hotfix for v0.3.4 migration error.

## Bug Fixes

- **Delete migration manifest**: Fixed field name in delete migration manifest — uses correct `from` field instead of `path` for Kilo commands cleanup

<Note>Run `trellis update` to apply the Kilo workflows migration that was broken in v0.3.4.</Note>
