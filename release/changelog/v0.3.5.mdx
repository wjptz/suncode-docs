---
title: 'v0.3.5'
description: '2026-03-05'
---

Hotfix for Kilo workflows delete migration.

## Bug Fixes

- **Migration manifest field name**: Fixed `delete` migration manifest using incorrect `path` field instead of `from`, causing Kilo commands cleanup to fail during `trellis update`

## Migration

No manual migration required. Run `trellis update` to apply the Kilo workflows migration that was blocked in v0.3.4.
