---
title: 'Appendix A: Key File Paths Quick Reference'
---

## Appendix A: Key File Paths Quick Reference

<Note>
  This appendix uses **Claude Code**'s directory layout as the example (`.claude/commands/`,
  `.claude/agents/`, `.claude/skills/`, `.claude/hooks/`). See the "Other platforms" section below,
  or chapter 13 for the full per-platform layout.
</Note>

### Core files

| File                                            | Description                           | Read on                           |
| ----------------------------------------------- | ------------------------------------- | --------------------------------- |
| `.suncode/workflow.md`                          | Development workflow contract         | Every session start               |
| `.suncode/config.yaml`                          | Packages, update.skip, task hooks     | Init and updates                  |
| `.suncode/.version`                             | Current Suncode version               | Updates                           |
| `.suncode/.template-hashes.json`                | Template file hashes                  | Updates                           |
| `.suncode/.developer`                           | Developer identity                    | Every session                     |
| `.suncode/.runtime/sessions/<session-key>.json` | Active task for one AI session/window | Continue, hooks, sub-agent launch |

### Shipped commands (Claude Code)

| File                                      | Invoked as             |
| ----------------------------------------- | ---------------------- |
| `.claude/commands/suncode/finish-work.md` | `/suncode:finish-work` |
| `.claude/commands/suncode/continue.md`    | `/suncode:continue`    |

On platforms where SessionStart is automatic, `start` is not installed as a user-facing command. Agent-less platforms still ship a manual start workflow.

### Shipped sub-agents (Claude Code)

| File                                  | Sub-agent           | Role                         |
| ------------------------------------- | ------------------- | ---------------------------- |
| `.claude/agents/suncode-implement.md` | `suncode-implement` | Writes code, no `git commit` |
| `.claude/agents/suncode-check.md`     | `suncode-check`     | Verify + self-fix            |
| `.claude/agents/suncode-research.md`  | `suncode-research`  | Read-only codebase search    |

### Shipped skills (Claude Code)

| File                                          | Skill                 | Triggers when…                           |
| --------------------------------------------- | --------------------- | ---------------------------------------- |
| `.claude/skills/suncode-brainstorm/SKILL.md`  | `suncode-brainstorm`  | User describes a feature / bug / request |
| `.claude/skills/suncode-before-dev/SKILL.md`  | `suncode-before-dev`  | About to write code in an active task    |
| `.claude/skills/suncode-check/SKILL.md`       | `suncode-check`       | Implementation finished                  |
| `.claude/skills/suncode-update-spec/SKILL.md` | `suncode-update-spec` | Worth capturing a learning               |
| `.claude/skills/suncode-break-loop/SKILL.md`  | `suncode-break-loop`  | A tricky bug was just resolved           |

### Hook scripts (Claude Code)

| File                                       | Trigger           | Function                  |
| ------------------------------------------ | ----------------- | ------------------------- |
| `.claude/hooks/session-start.py`           | SessionStart      | Auto-inject context       |
| `.claude/hooks/inject-workflow-state.py`   | UserPromptSubmit  | Workflow-state breadcrumb |
| `.claude/hooks/inject-subagent-context.py` | PreToolUse (Task) | Spec injection engine     |

### Other platforms

Each platform writes into its own directory (see chapter 13 for the full layout):

- `.cursor/commands/`, `.cursor/skills/`, `.cursor/agents/`, `.cursor/hooks/`, `.cursor/hooks.json`, `.cursor/rules/suncode.mdc`
- `.opencode/commands/suncode/`, `.opencode/agents/`, `.opencode/skills/`, `.opencode/plugins/`
- `.engineer/commands/suncode/`, `.engineer/agents/`, `.engineer/skills/`, `.engineer/plugins/`
- `.codex/prompts/`, `.codex/skills/`, `.codex/agents/` (TOML), `.codex/hooks/`, `AGENTS.md`
- `.kiro/steering/`, `.kiro/prompts/`, `.kiro/skills/`, `.kiro/agents/`
- `.gemini/commands/suncode/` (TOML), `.agents/skills/`, `.gemini/agents/`, `.gemini/hooks/`
- `.qoder/commands/`, `.qoder/skills/`, `.qoder/agents/`, `.qoder/hooks/`
- `.codebuddy/commands/suncode/`, `.codebuddy/skills/`, `.codebuddy/agents/`, `.codebuddy/hooks/`
- `.factory/commands/suncode/`, `.factory/droids/`, `.factory/skills/`, `.factory/hooks/`
- `.github/copilot-instructions.md`, `.github/prompts/`, `.github/skills/`, `.github/agents/`, `.github/copilot/hooks/`
- `.pi/prompts/`, `.pi/skills/`, `.pi/agents/`, `.pi/extensions/suncode/`, `.pi/settings.json`
- `.kilocode/workflows/`, `.kilocode/skills/`
- `.agent/workflows/`, `.agent/skills/` (Antigravity)
- `.devin/workflows/`, `.devin/skills/`
- `.reasonix/skills/`
- `.zcode/commands/suncode/`, `.zcode/cli/agents/`, `.agents/skills/`
- `.trae/commands/`, `.trae/skills/`, `.trae/agents/`, `.trae/hooks/`, `.trae/hooks.json`
- `.agents/skills/` (shared cross-platform layer, written by Codex configurator)

### Scripts

| Script                                 | Function                   |
| -------------------------------------- | -------------------------- |
| `.suncode/scripts/task.py`             | Task management            |
| `.suncode/scripts/get_context.py`      | Session context            |
| `.suncode/scripts/add_session.py`      | Record session             |
| `.suncode/scripts/create_bootstrap.py` | First-time spec bootstrap  |
| `.suncode/scripts/get_developer.py`    | Developer identity utility |
| `.suncode/scripts/init_developer.py`   | Developer initialization   |

---
