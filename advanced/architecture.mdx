---
title: 'Architecture Overview'
---

Suncode is a **Team-level Agent Harness with built-in LLM wiki**.

In implementation terms, that means two systems share the same project files:

- **Agent Harness**: workflow state, hooks, skills, sub-agents, and platform adapters that control how AI coding work moves.
- **Built-in LLM wiki**: specs, tasks, research, and journals stored in the repository so AI sessions can reload project knowledge from files.
- **Team-level layer**: git-tracked workflow/spec/task files plus per-developer workspace memory, so multiple people and multiple AI tools operate against the same conventions.

This document maps that idea to the current Suncode feature set, the modules that implement each feature, and the files to inspect when customizing a generated project. For the user flow, see [How It Works](/start/how-it-works).

> Suncode is AGPL-3.0 licensed. Review the repository license before distributing a modified product or hosted derivative.

## Design principles

Suncode treats AI coding as a workflow and knowledge-management problem, not a single chat session.

| Principle                        | Implementation rule                                                                                                                                                                                               |
| -------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Make workflow state explicit     | Task `status` and `[workflow-state:STATUS]` blocks tell the main session what phase it is in.                                                                                                                     |
| Plan once, execute by capability | `execution.json` is the canonical DAG for inline, native sub-agent, and channel executors; only concurrency and isolation capabilities differ.                                                                    |
| Store durable knowledge in files | Requirements, specs, research, and journals live under `.suncode/`, not only in conversation history.                                                                                                             |
| Split work by responsibility     | Research, implementation, and checking use separate agents or skills depending on platform capability.                                                                                                            |
| Keep context scoped              | JSONL manifests list the spec/research files needed for the current task instead of dumping the whole repo.                                                                                                       |
| Preserve the review boundary     | Implement/check agents produce a clean diff; the main session proposes commits, and `/suncode:finish-work` only archives and journals.                                                                            |
| Support teams and tool diversity | The same `.suncode/` model is adapted into Claude Code, Cursor, OpenCode, Engineer, Codex, Kiro, Gemini, Qoder, Copilot, Droid, Pi, Reasonix, ZCode, Trae, Snow CLI, Kilo, Antigravity, Devin, and related tools. |

## Feature overview

### Agent Harness

| Feature                            | What it does                                                                                                                          |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| Three-phase workflow               | Defines Plan, Execute, and Finish in `.suncode/workflow.md`.                                                                          |
| Per-turn workflow-state breadcrumb | Injects the current next-action rule into the main session on hook-capable platforms.                                                 |
| Task lifecycle                     | Stores requirements, status, assignee, branch, PR metadata, and subtask relationships in `.suncode/tasks/<task>/`.                    |
| Recoverable execution DAG          | Schedules dependency-ready nodes with scope/resource conflict checks, immutable contexts, structured results, and persisted recovery. |
| Research / implement / check roles | Separates investigation, code writing, and verification.                                                                              |
| Read-before-write enforcement      | Makes implement/check paths read PRD and relevant specs before changing files.                                                        |
| Finish boundary                    | Separates final verification, spec update, work commit, task archive, and journal writing.                                            |

### Built-in LLM wiki

| Feature                 | What it stores                                                                                        |
| ----------------------- | ----------------------------------------------------------------------------------------------------- |
| Spec library            | Project conventions and thinking guides under `.suncode/spec/`.                                       |
| Task knowledge          | PRDs, technical designs, implementation plans, research, and JSONL manifests under `.suncode/tasks/`. |
| Workspace memory        | Per-developer journals under `.suncode/workspace/<developer>/`.                                       |
| Workflow documentation  | The executable workflow contract in `.suncode/workflow.md`.                                           |
| Local customization map | Bundled `suncode-meta` references explaining which generated files own each behavior.                 |

### Team-level behavior

| Feature                              | What it enables                                                                                           |
| ------------------------------------ | --------------------------------------------------------------------------------------------------------- |
| Git-tracked workflow/spec/task files | New team members clone the same workflow and conventions.                                                 |
| Per-session active task pointers     | Multiple windows can work on different tasks without sharing one global current task.                     |
| Subtask trees                        | Parent tasks can hold shared requirements while child tasks run their own Plan -> Execute -> Finish loop. |
| Cross-platform adapters              | Teams can use different AI tools while keeping the same `.suncode/` facts.                                |
| Spec distillation                    | Lessons from one task can be promoted into `.suncode/spec/` for future tasks.                             |

## Feature to module map

| Feature                            | Primary module          | Local files to inspect                                                                                                                                                      |
| ---------------------------------- | ----------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Workflow phases and routing        | Workflow module         | `.suncode/workflow.md`                                                                                                                                                      |
| Per-turn next-action injection     | Workflow-state module   | `[workflow-state:STATUS]` blocks, `inject-workflow-state.py` or equivalent plugin                                                                                           |
| Task creation, status, archive     | Task store module       | `.suncode/tasks/<task>/task.json`, `.suncode/scripts/task.py`, `.suncode/scripts/common/task_store.py`                                                                      |
| Session-scoped current task        | Active-task runtime     | `.suncode/.runtime/sessions/<session-key>.json`, `.suncode/scripts/common/active_task.py`                                                                                   |
| DAG planning and scheduling        | Execution runtime       | `execution.json`, `.suncode/scripts/common/execution_model.py`, `execution_runtime.py`, `execution_context.py`, `task_execution.py`                                         |
| Planning artifact/spec injection   | Context-loading module  | `prd.md`, `design.md`, `implement.md`, `implement.jsonl`, `check.jsonl`, `inject-subagent-context.py`, platform agent preludes                                              |
| Research / implement / check roles | Agent and skill module  | Platform `agents/`, `skills/`, prompts, workflows                                                                                                                           |
| Team convention memory             | Spec module             | `.suncode/spec/**/index.md`, guideline files                                                                                                                                |
| Developer memory                   | Workspace module        | `.suncode/workspace/<developer>/journal-*.md`                                                                                                                               |
| Platform support                   | Platform adapter module | `.claude/`, `.cursor/`, `.opencode/`, `.engineer/`, `.codex/`, `.kiro/`, `.gemini/`, `.qoder/`, `.github/`, `.pi/`, `.reasonix/`, `.zcode/`, `.trae/`, and related settings |
| Finish and archive                 | Finish module           | `/suncode:finish-work`, `.suncode/scripts/add_session.py`, task archive scripts                                                                                             |

## Workflow module

`.suncode/workflow.md` is the source of truth for Suncode's Plan -> Execute -> Finish contract.

```text
Phase 1: Plan    -> classify the turn, get task-creation consent, write planning artifacts
Phase 2: Execute -> run dependency-ready nodes, converge on the final check barrier
Phase 3: Finish  -> final verification, spec update, work commit, archive
```

The file owns three things:

- phase definitions and numbered steps
- skill / sub-agent routing by platform capability
- `[workflow-state:STATUS]` blocks used by the per-turn breadcrumb hook

Changing workflow behavior starts here. Platform commands, skills, and agent files may also need wording updates if they describe the same flow, but the workflow contract itself belongs in `.suncode/workflow.md`.

## Workflow-state module

On hook-capable platforms, `inject-workflow-state.py` or the equivalent plugin runs on each user prompt.

The runtime contract is:

1. Resolve the Suncode root from `cwd`.
2. Resolve the active task for the current session.
3. Read `task.json.status`, or synthesize `no_task` when no task is active.
4. Parse `.suncode/workflow.md`.
5. Inject the matching `[workflow-state:STATUS]` body into `<workflow-state>...</workflow-state>`.

Marker syntax:

```text
[workflow-state:planning]
...
[/workflow-state:planning]
```

The hook scripts are parser-only. They do not contain fallback copies of the breadcrumb text. If a matching block is missing, the hook emits `Refer to workflow.md for current step.`

Default statuses:

| Status        | Writer                  | Notes                                                                                                                                                              |
| ------------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `no_task`     | Synthesized by the hook | No active task pointer for the current session.                                                                                                                    |
| `planning`    | `task.py create`        | Requirements and planning artifacts are active; DAG-enabled tasks add a reviewed `execution.json`, with optional complex-task enforcement snapshotted at creation. |
| `in_progress` | `task.py start`         | Implementation, checking, and finish steps are active.                                                                                                             |
| `completed`   | `task.py archive`       | Written immediately before archive move; not normally visible as a live breadcrumb.                                                                                |

`task.py create` best-effort sets the current session's active-task pointer, so `planning` is reachable during brainstorm and JSONL curation.

## Task store module

Each task is one directory:

```text
.suncode/tasks/<MM-DD-slug>/
├── task.json
├── prd.md
├── design.md
├── implement.md
├── execution.json
├── implement.jsonl
├── check.jsonl
└── research/
```

Important files:

| File              | Purpose                                                                                                                             |
| ----------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| `task.json`       | Status, priority, assignee, branch, PR URL, parent/child relationships, and extension metadata.                                     |
| `prd.md`          | Requirements, constraints, acceptance criteria, and out-of-scope items.                                                             |
| `design.md`       | Technical design for complex tasks: boundaries, contracts, data flow, compatibility, tradeoffs.                                     |
| `implement.md`    | Execution plan for complex tasks: ordered checklist, validation commands, review gates, rollback points.                            |
| `execution.json`  | Canonical versioned dependency DAG: roles, scopes, resources, validation, context budgets, executor constraints, and final barrier. |
| `implement.jsonl` | Spec and research manifest for implementation context.                                                                              |
| `check.jsonl`     | Spec and research manifest for review and verification context.                                                                     |
| `research/`       | Research artifacts written by `suncode-research` or the main planning flow.                                                         |

Task lifecycle hooks are command events, not generic status watchers:

| Lifecycle event | Fires when                                  | Meaning                                                                                |
| --------------- | ------------------------------------------- | -------------------------------------------------------------------------------------- |
| `after_create`  | `task.py create` finishes                   | A task directory exists.                                                               |
| `after_start`   | `task.py start` finishes                    | The task entered `in_progress`.                                                        |
| `after_finish`  | `task.py finish` clears the session pointer | The current AI session detached from the task; the task may still be active elsewhere. |
| `after_archive` | `task.py archive` finishes                  | The task is archived; use this for external "done" sync.                               |

## Active-task runtime module

The current task is session-scoped:

```text
.suncode/.runtime/sessions/<session-key>.json
```

That file points one AI session or window at one task. Different windows can work on different tasks in the same repository.

`.suncode/.current-task` is a fallback for command-line contexts. Session-scoped runtime pointers take precedence when the platform provides a session identity.

## Execution runtime module

Planning produces one executor-neutral `execution.json`. A lightweight task may
use one node; a complex graph must preserve safe independent branches and make
every branch feed the final integration/check barrier. `implement.md` remains
the human/legacy serial view, while `subtasks.json` is only a Hub display
override.

`task.py execution start-run` freezes the plan hash and executor capabilities.
The ready-set scheduler selects a maximal conflict-free fan-out based on real
dependencies, read/write glob overlap, resource locks, role support, global
shared-worktree locks, and available capacity. A coordinator claims and
dispatches the whole selected set before waiting, records the first returned
`NodeResult` v1, then immediately recomputes readiness. Inline follows the same
algorithm with concurrency one.

Bundled v0.6.12 executors use `shared-worktree` isolation. The plan schema also
accepts `worktree` and `sandbox` for custom adapters, but declaring either value
does not copy runtime manifests or content into another checkout; that adapter
must provide artifact transport.

Runtime state lives under `.suncode/.runtime/execution/`: state, append-only
events, attempts, immutable context packages, structured results, and declared
artifacts. Plan-hash drift blocks resume. Recovery reconciles finished results
and marks lost workers orphaned; automatic retry is limited to nodes declared
safe for retry.

## Context-loading module

Suncode loads context through three paths:

| Context type             | Source                                                                                  | Used by                                             |
| ------------------------ | --------------------------------------------------------------------------------------- | --------------------------------------------------- |
| Startup context          | `.suncode/scripts/get_context.py`                                                       | Main session startup report.                        |
| Per-turn workflow state  | `.suncode/workflow.md` blocks                                                           | Main session next-action guidance.                  |
| Claimed node context     | `.suncode/.runtime/execution/**/contexts/**/manifest.json` and `content.md`             | Inline, native sub-agent, and channel node workers. |
| Legacy task/spec context | `prd.md`, `design.md`, `implement.md`, `implement.jsonl`, `check.jsonl`, research files | Dispatches without a claimed execution manifest.    |

JSONL rows are plain file references:

```text
{"file": ".suncode/spec/docs-site/docs/style-guide.md", "reason": "Docs writing style"}
```

Manifest-first dispatch is the standard flow. The context builder combines the
node contract, explicitly selected task artifacts/JSONL sources, and structured
results from direct succeeded dependencies. It enforces repository containment,
secret-path denial and redaction, per-source/total byte budgets, deterministic
ordering, truncation records, and SHA-256 integrity. The parent conversation is
not a context source. Legacy dispatch without a manifest keeps the earlier
`jsonl entries -> prd.md -> design.md -> implement.md` fallback.

## Platform adapter module

Suncode uses each platform's available primitives. The generated project files are the source of truth for an actual repository, but the default capability groups are:

| Group                          | Platforms                                                     | Startup context                                                         | Per-turn workflow-state                                           | Implementation/check context                                                                 |
| ------------------------------ | ------------------------------------------------------------- | ----------------------------------------------------------------------- | ----------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| Hook + hook-push sub-agent     | Claude Code, Cursor, OpenCode, Engineer, CodeBuddy, Droid, Pi | Hook, plugin, or extension                                              | Hook or equivalent                                                | Hook injects JSONL entries, `prd.md`, `design.md` if present, and `implement.md` if present. |
| Hook + pull-prelude sub-agent  | Gemini CLI, Qoder, Copilot, Trae                              | Hook                                                                    | Hook                                                              | Sub-agent reads JSONL entries and task artifacts itself on startup.                          |
| No-hook pull-prelude sub-agent | Reasonix, ZCode                                               | Manual command or skill entry                                           | none                                                              | Sub-agent reads JSONL entries and task artifacts itself on startup.                          |
| Codex                          | Codex                                                         | `AGENTS.md`; `UserPromptSubmit` can inject a no-task bootstrap reminder | Optional hook when enabled (0.129+ also requires `/hooks` review) | Inline mode reads artifacts through skills; sub-agent mode uses pull-based preludes.         |
| Kiro                           | Kiro                                                          | Skill files under `.kiro/`                                              | No Suncode per-turn hook by default                               | Agent/skill files read Suncode context.                                                      |
| Main-session workflow/skill    | Kilo, Antigravity, Devin                                      | Manual workflow or skill entry                                          | No Suncode per-turn hook by default                               | Main session reads specs and task files inline.                                              |

If the local settings file disagrees with this table, follow the local settings file. Suncode projects are intentionally customizable.

## Agent and skill module

Suncode ships three sub-agent roles when the platform supports sub-agents:

| Sub-agent           | Role                                                                                      | Primary context                                                                   |
| ------------------- | ----------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| `suncode-research`  | Investigate code, docs, APIs, and alternatives; write findings under `research/`.         | Research prompt plus task/repo context as needed.                                 |
| `suncode-implement` | Write the change against the planning artifacts and relevant specs.                       | `implement.jsonl`, `prd.md`, optional `design.md`, optional `implement.md`.       |
| `suncode-check`     | Review and run checks; DAG check nodes report read-only and route writes to one fix node. | Claimed node manifest, with the JSONL/task-artifact fallback for legacy dispatch. |

Skills cover phases where the main session needs guidance: brainstorm, before-dev, check, update-spec, finish-work, and meta customization. On platforms without sub-agents, skills carry more of the execution path directly in the main session.

Removed 0.4 mechanisms:

- `dispatch`, `plan`, and `debug` sub-agents were replaced by skill routing.
- The SubagentStop-based Ralph Loop was replaced by `suncode-check` owning its retry loop.
- Suncode no longer ships its own `/parallel` worktree orchestrator; use the platform's native worktree support.

## LLM wiki modules

The wiki side is a set of repository files that AI sessions can reread.

| Module                 | Path                              | Contents                                                                           |
| ---------------------- | --------------------------------- | ---------------------------------------------------------------------------------- |
| Spec library           | `.suncode/spec/`                  | Team conventions, package/layer rules, thinking guides.                            |
| Task knowledge         | `.suncode/tasks/`                 | PRDs, technical designs, implementation plans, research, JSONL manifests, archive. |
| Workspace memory       | `.suncode/workspace/<developer>/` | Per-developer journals and indexes.                                                |
| Workflow reference     | `.suncode/workflow.md`            | The workflow contract and next-action prompt blocks.                               |
| Suncode meta reference | bundled `suncode-meta` skill      | Local architecture and customization map for generated Suncode files.              |

Stable team rules belong in `.suncode/spec/`. Task-specific facts belong in the task directory. Session notes belong in `.suncode/workspace/<developer>/`.

## Finish module

Suncode separates implementation, work commits, and bookkeeping:

1. Implement/check agents produce a clean diff.
2. The main session runs final verification and `suncode-update-spec`.
3. Phase 3.4 proposes a batched commit plan, waits for one user confirmation, stages the listed files, and runs `git commit`. It does not amend and does not push.
4. `/suncode:finish-work` classifies dirty paths, stops if current-task work is still uncommitted, archives the task, and writes the workspace journal.

`/suncode:finish-work` is not the command that commits feature code. Work commits happen first; archive and journal commits are bookkeeping after that.

## Generated and protected files

`suncode init` and `suncode update` generate local files, but local edits matter:

| Path                             | Rule of thumb                                                      |
| -------------------------------- | ------------------------------------------------------------------ |
| `.suncode/workflow.md`           | Project workflow source of truth; update deliberately.             |
| `.suncode/spec/`                 | Team-owned; template updates do not overwrite package/layer specs. |
| `.suncode/tasks/`                | Work history; avoid manually deleting active tasks.                |
| Platform directories             | Tool adapters; inspect local settings before changing behavior.    |
| `.suncode/.template-hashes.json` | Managed hash index; edit only when repairing update state.         |
| `.suncode/.runtime/`             | Runtime state; normally not edited by hand.                        |

For local customization, use the bundled `suncode-meta` skill as the map. It tells an AI to read local architecture references first, inspect actual project files, and modify the project copy instead of changing `node_modules` or a global install.

## Customization map

| Goal                                                       | First file to inspect                                                                           |
| ---------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| Change phase order, required steps, or next-action wording | `.suncode/workflow.md`                                                                          |
| Change task creation, status, archive, or lifecycle sync   | `.suncode/scripts/common/task_store.py`, `.suncode/scripts/task.py`, `.suncode/config.yaml`     |
| Change spec loading or JSONL rules                         | `.suncode/workflow.md`, `.suncode/scripts/common/task_context.py`, platform agent/prelude files |
| Change hook behavior                                       | Platform hook settings plus `.suncode/scripts/` hook implementations                            |
| Change implement/check agent behavior                      | Platform agent files and task JSONL context rules                                               |
| Add team-specific coding rules                             | `.suncode/spec/` or a project-local skill                                                       |
