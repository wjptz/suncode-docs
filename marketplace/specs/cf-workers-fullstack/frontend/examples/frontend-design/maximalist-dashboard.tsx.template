// Maximalist Dashboard - Example Template
// Demonstrates rich visual layers, bold colors, and atmospheric depth

import { Card } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { Sparkles, TrendingUp, Zap } from 'lucide-react';

export function MaximalistDashboard() {
  return (
    <div className="min-h-screen bg-slate-950 relative overflow-hidden">
      {/* Complex layered background */}
      <div className="absolute inset-0">
        {/* Gradient mesh layer 1 */}
        <div className="absolute inset-0 bg-gradient-to-br from-violet-600/20 via-fuchsia-500/10 to-cyan-500/20" />

        {/* Gradient mesh layer 2 - offset */}
        <div className="absolute inset-0 bg-gradient-to-tl from-amber-500/10 via-transparent to-emerald-500/10" />

        {/* Noise texture overlay */}
        <div className="absolute inset-0 opacity-30 mix-blend-overlay bg-[url('data:image/svg+xml;base64,')]" />

        {/* Animated gradient orbs */}
        <div className="absolute top-1/4 left-1/4 w-96 h-96 bg-violet-500/30 rounded-full blur-3xl animate-pulse" />
        <div className="absolute bottom-1/4 right-1/4 w-96 h-96 bg-fuchsia-500/30 rounded-full blur-3xl animate-pulse" style={{ animationDelay: '1s' }} />
      </div>

      {/* Content */}
      <div className="relative z-10 p-8">
        {/* Header with dramatic typography */}
        <header className="mb-12 animate-fade-in-up">
          <h1 className="text-6xl font-black text-transparent bg-clip-text bg-gradient-to-r from-violet-400 via-fuchsia-400 to-cyan-400 mb-2">
            Dashboard
          </h1>
          <p className="text-slate-400 text-lg font-medium tracking-wide">
            Your command center for everything
          </p>
        </header>

        {/* Grid of cards with staggered animations */}
        <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
          {/* Card 1 - with dramatic shadow and border */}
          <Card
            className="bg-slate-900/50 backdrop-blur-xl border-violet-500/20 shadow-2xl shadow-violet-500/10 hover:shadow-violet-500/20 transition-all duration-500 p-6 animate-fade-in-up group"
            style={{ animationDelay: '100ms' }}
          >
            <div className="flex items-start justify-between mb-4">
              <div className="p-3 rounded-xl bg-gradient-to-br from-violet-500 to-fuchsia-500 shadow-lg shadow-violet-500/50">
                <Sparkles className="h-6 w-6 text-white" />
              </div>
              <span className="text-xs uppercase tracking-wider text-violet-400 font-bold">
                Active
              </span>
            </div>

            <h3 className="text-3xl font-bold text-white mb-2 group-hover:text-violet-400 transition-colors">
              2,847
            </h3>
            <p className="text-slate-400 text-sm">Total Engagements</p>

            {/* Decorative gradient line */}
            <div className="mt-6 h-1 w-full bg-gradient-to-r from-violet-500 via-fuchsia-500 to-transparent rounded-full" />
          </Card>

          {/* Card 2 */}
          <Card
            className="bg-slate-900/50 backdrop-blur-xl border-cyan-500/20 shadow-2xl shadow-cyan-500/10 hover:shadow-cyan-500/20 transition-all duration-500 p-6 animate-fade-in-up group"
            style={{ animationDelay: '200ms' }}
          >
            <div className="flex items-start justify-between mb-4">
              <div className="p-3 rounded-xl bg-gradient-to-br from-cyan-500 to-blue-500 shadow-lg shadow-cyan-500/50">
                <TrendingUp className="h-6 w-6 text-white" />
              </div>
              <span className="text-xs uppercase tracking-wider text-cyan-400 font-bold">
                Growth
              </span>
            </div>

            <h3 className="text-3xl font-bold text-white mb-2 group-hover:text-cyan-400 transition-colors">
              +127%
            </h3>
            <p className="text-slate-400 text-sm">Monthly Increase</p>

            <div className="mt-6 h-1 w-full bg-gradient-to-r from-cyan-500 via-blue-500 to-transparent rounded-full" />
          </Card>

          {/* Card 3 */}
          <Card
            className="bg-slate-900/50 backdrop-blur-xl border-amber-500/20 shadow-2xl shadow-amber-500/10 hover:shadow-amber-500/20 transition-all duration-500 p-6 animate-fade-in-up group"
            style={{ animationDelay: '300ms' }}
          >
            <div className="flex items-start justify-between mb-4">
              <div className="p-3 rounded-xl bg-gradient-to-br from-amber-500 to-orange-500 shadow-lg shadow-amber-500/50">
                <Zap className="h-6 w-6 text-white" />
              </div>
              <span className="text-xs uppercase tracking-wider text-amber-400 font-bold">
                Live
              </span>
            </div>

            <h3 className="text-3xl font-bold text-white mb-2 group-hover:text-amber-400 transition-colors">
              98.7%
            </h3>
            <p className="text-slate-400 text-sm">System Uptime</p>

            <div className="mt-6 h-1 w-full bg-gradient-to-r from-amber-500 via-orange-500 to-transparent rounded-full" />
          </Card>
        </div>

        {/* CTA section with dramatic styling */}
        <div
          className="mt-12 p-8 rounded-2xl bg-gradient-to-r from-violet-600/20 via-fuchsia-600/20 to-cyan-600/20 backdrop-blur-xl border border-violet-500/20 shadow-2xl animate-fade-in-up"
          style={{ animationDelay: '400ms' }}
        >
          <h2 className="text-3xl font-bold text-white mb-4">
            Ready to level up?
          </h2>
          <p className="text-slate-300 mb-6 max-w-2xl">
            Unlock premium features and take your experience to the next dimension.
          </p>
          <Button
            size="lg"
            className="bg-gradient-to-r from-violet-500 via-fuchsia-500 to-cyan-500 text-white font-bold shadow-lg shadow-violet-500/50 hover:shadow-xl hover:shadow-violet-500/70 transition-all duration-300"
          >
            Upgrade Now
          </Button>
        </div>
      </div>
    </div>
  );
}

// Key Design Choices:
// - Color scheme: Bold neon gradients (violet, fuchsia, cyan, amber)
// - Layering: Multiple background gradients, noise texture, animated orbs
// - Typography: Bold, large scale with gradient text effects
// - Shadows: Dramatic, colored shadows matching accent colors
// - Animation: Staggered card reveals, pulsing orbs, smooth transitions
// - Depth: backdrop-blur, transparency layers, overlapping elements
// - Unforgettable element: The layered, animated gradient background
