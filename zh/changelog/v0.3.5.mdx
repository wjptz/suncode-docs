---
title: 'v0.3.5'
description: '2026-03-05'
---

修复 Kilo workflows 删除迁移。

## Bug 修复

- **Migration manifest 字段名**：修复 `delete` 迁移 manifest 使用了错误的 `path` 字段（应为 `from`），导致 `trellis update` 时 Kilo commands 清理迁移失败

## 迁移

无需手动操作。运行 `trellis update` 即可执行在 v0.3.4 中被阻塞的 Kilo workflows 迁移。
