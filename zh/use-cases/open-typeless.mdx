---
title: 'open-typeless'
description: '手把手教程：用 Trellis 开发 macOS 语音输入应用'
---

# open-typeless

一个手把手教程，展示如何用 Trellis 从零开始构建 macOS 语音输入应用。

<Info>
  **源代码**: [github.com/mindfold-ai/open-typeless](https://github.com/mindfold-ai/open-typeless)
</Info>

## 项目初始化

### 创建 Electron 项目

```bash
npx create-electron-app@latest open-typeless --template=vite-typescript

cd open-typeless

# 删除 npm 生成的文件
rm -rf node_modules package-lock.json

# 创建 .npmrc (pnpm + Electron 必需)
cat > .npmrc << 'EOF'
node-linker=hoisted
shamefully-hoist=true
EOF

# 用 pnpm 重新安装
pnpm install
```

### 初始化 Trellis

```bash
trellis init
```

![trellis init](/images/use-cases/open-typeless/init.png)

### 从旧项目复制 Spec

如果有类似项目的 spec，可以复制过来：

```bash
cp -r /path/to/old-project/.trellis/spec ./
```

### 让 AI 填充 Spec

**Prompt:**

> 帮我从 electron-doc/ 里选择有用的 spec，整理到本项目的 .trellis/spec/ 里

AI 会分析并组织 spec：

![spec selection](/images/use-cases/open-typeless/step1.png)

## 任务规划

### 让 AI 规划任务

**Prompt:**

> 我打算用火山引擎 ASR BigModel API 来做这个。帮我规划一下怎么拆分任务。

AI 创建基于批次的任务计划：

![task planning](/images/use-cases/open-typeless/step2-1.png)

### 创建任务

AI 把任务组织成批次：

| 批次    | 任务                                                                 | 目的                   |
| ------- | -------------------------------------------------------------------- | ---------------------- |
| Batch 1 | `asr-infrastructure`                                                 | 基础设施（必须先完成） |
| Batch 2 | `asr-audio-recorder`, `asr-volcengine-client`, `asr-floating-window` | 可以并行开发           |
| Batch 3 | `asr-integration`                                                    | 集成（依赖 Batch 2）   |

![tasks created](/images/use-cases/open-typeless/step2-2.png)

### 完成 Batch 1

Batch 1 完成后，验证并更新下游任务的 context：

![batch 1 complete](/images/use-cases/open-typeless/step2-3.png)

## 并行开发

### 启动并行会话

当前 Trellis 推荐为 Batch 2 的每个任务创建一个 Git worktree 和一个 AI 会话，然后在对应会话里 start 对应 Trellis task。

```bash
git worktree add ../asr-audio-recorder -b feature/asr-audio-recorder
git worktree add ../asr-volcengine-client -b feature/asr-volcengine-client
git worktree add ../asr-floating-window -b feature/asr-floating-window
```

每个会话使用自己的 `.trellis/.runtime/sessions/<session-key>.json`，一个任务的 start 不会影响另一个会话。

![parallel agents](/images/use-cases/open-typeless/step3-1.png)

## 监控进度

### 查看 Agent 状态

AI 监控 agent 状态和任务进度：

![agent status](/images/use-cases/open-typeless/step4-1.png)

### 记录 Session

并行会话完成后，用你平时的 Git 流程 review 和 merge 每个分支：

![parallel PRs](/images/use-cases/open-typeless/step3-4.png)

合并并完成批次后，记录 session：

**Prompt:** `/trellis:finish-work`

![record session](/images/use-cases/open-typeless/step2-4.png)

## 继续开发

### 查看剩余任务

AI 显示当前项目的剩余任务：

![task list](/images/use-cases/open-typeless/step5-1.png)

### 实现下一个功能

选择下一个任务，AI 用 trellis-implement sub-agent 然后 trellis-check sub-agent：

![implement and check](/images/use-cases/open-typeless/step5-2.png)

### 配置和测试

AI 帮助完成剩余设置（环境配置、权限）：

![final setup](/images/use-cases/open-typeless/step5-3.png)

## 总结

用 Trellis 构建 open-typeless：

| 步骤 | 做什么     | Trellis 功能                         |
| ---- | ---------- | ------------------------------------ |
| 1    | 初始化项目 | `trellis init`，spec 组织            |
| 2    | 规划任务   | AI 任务拆分，批次规划                |
| 3    | 并行开发   | 原生 Git worktree + session-scoped Trellis tasks |
| 4    | 监控和记录 | `/trellis:finish-work`            |
| 5    | 继续迭代   | Task hooks，implement/trellis-check sub-agents   |

**结果：** 1 天完成完整 Electron 应用，有结构化的 spec 和文档化的进度。
