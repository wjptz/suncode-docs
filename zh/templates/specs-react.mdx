---
title: 'React + TypeScript'
description: 'React + TypeScript 前端规范模板'
---

## 项目结构

```
src/
├── components/  # 复用组件
├── pages/       # 路由页面
├── hooks/       # 自定义 hooks
├── stores/      # Zustand stores
├── lib/         # 工具
├── types/       # TypeScript 类型
└── api/         # API 客户端函数
```

## 组件

只用命名导出。不用默认导出。

```tsx
// src/components/UserCard.tsx
interface UserCardProps {
  user: User;
  onEdit?: () => void;
}

export function UserCard({ user, onEdit }: UserCardProps) {
  return (
    <div className="user-card">
      <span>{user.name}</span>
      {onEdit && <button onClick={onEdit}>Edit</button>}
    </div>
  );
}
```

Props 接口定义在组件上面。不用 inline 类型。

## 状态管理

用 Zustand。Store 文件在 `src/stores/`：

```tsx
// src/stores/user.ts
import { create } from 'zustand';

interface UserStore {
  user: User | null;
  setUser: (user: User) => void;
  logout: () => void;
}

export const useUserStore = create<UserStore>((set) => ({
  user: null,
  setUser: (user) => set({ user }),
  logout: () => set({ user: null }),
}));
```

## API 调用

API 函数在 `src/api/`。用 `src/lib/fetch.ts` 的 fetch 封装：

```tsx
// src/api/users.ts
import { api } from '../lib/fetch';

export async function getUser(id: string): Promise<User> {
  return api.get(`/users/${id}`);
}
```

## 样式

Tailwind CSS。不用 inline styles。不用 CSS modules。

复杂组件提取 class names：

```tsx
const cardStyles = 'rounded-lg shadow-md p-4 bg-white';
const titleStyles = 'text-lg font-semibold text-gray-900';
```
